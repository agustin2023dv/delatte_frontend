// App.tsx

/**
 * 🚀 Entry point de `expo-router`.
 * No debe incluir providers ni navegación personalizada.
 * Toda la lógica de providers se maneja dentro de `/src/app/_layout.tsx`.
 */

import 'expo-router/entry';
