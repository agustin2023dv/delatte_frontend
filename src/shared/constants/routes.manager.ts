// src/shared/constants/routes.manager.ts

  
  export const MANAGER_ROUTES = {
    HOME : '/(manager)/home',
    PROFILE: '/(manager)/profile',
    NOTIFICACIONS: '/(manager)/profile',
    RESTAURANTS: '/(manager)/restaurants',
  } as const;
  